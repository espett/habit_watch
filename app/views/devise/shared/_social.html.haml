- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to (image_tag("#{provider.to_s}.png", :alt =>  "#{provider.to_s}")), omniauth_authorize_path(resource_name, provider), :class => "#{provider.to_s}"
    %br/