= simple_form_for(@habit) do |f|
  = f.error_notification
  = f.input :name
  = f.input :description
  = f.input :type_of_habit, collection: ['good', 'bad', 'nuetral'], as: :radio_buttons
  = f.submit 'Submit', :class => 'button'
