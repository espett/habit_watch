%nav.top-bar
  %ul.title-area
    %li.name
      %h1
        = link_to "Habit Watch", root_path, :class => 'brand'
  - if user_signed_in?
    %li
      = link_to 'Logout', logout_path, :method=>'delete'
  - else
    %li
      = link_to 'Login', login_path
  - if user_signed_in?
    %li
      = link_to 'Settings', settings_path
  - else
    %li
      = link_to 'Sign up', signup_path
